const state = {
  index:0
};
// getters
const getters = {
  link: state => state.index,
};

const actions = {};
// mutations
const mutations = {

};
export default {
  state,
  getters,
  actions,
  mutations
}
